

StudentSubjectEnrollmentId id = new StudentSubjectEnrollmentId()

Доброто е тоа што постоењето на UUID го прави објектот не-енумерабилен, што ја подобрува безбедноста. Дополнително, истиот е и type-safe, што значи нема мешање со други ID-а кои што се атрибути на објектите во класите.

Единствено подобрување кое што би го препорачала е креирање на конструктор со параметри, наместо конструктор без параметри, бидејќи конструкторот без параметри може да биде ризичен.

Пример: public static StudentSubjectEnrollmentId fromString(String value) ------

        одговор: Додаден нови методи во studentSubjectEnrollment и GroupId:  public static StudentSubjectEnrollmentId fromString(String value) {
                                                                             return new StudentSubjectEnrollmentId(value);
                                                                               }
                                                                                public static GroupId fromString(String value) {
                                                                                        return new GroupId(value);
                                                                                    }

InvalidNote note = new InvalidNote("Detailed reason...")

Добро е тоа што има проверка за должината на текстот, што спречува погрешни или garbage внесувања. Исто така, применување на whitelist може да спречи SQL injection. Дополнително, имплементацијата на методот е fail-fast, што значи било какви грешки може да бидат приметени веднаш.

Подобрувањата кои што можат да бидат направени се следните: Regex whitelist не е дозволен за SQL injection, поради тоа што на DB layer треба да има parameterized queries, и лимитот поставен на 4000 карактери е прилично голем, можеби би било подобро доколку истиот се намали, на пример од 500 до 1000 карактери.

private static final int MAX_LENGTH = 1000;


        одговор: Променето е од 4000 на 1000 карактери долзината.


NumberOfEnrollments num = new NumberOfEnrollments(5);

Ова е одлично применет атрибут. Добро е тоа што има јасно дефиниран ранг од 1 до 10, што исто така игра улога како domain rule. Уште повеќе, јака страна на атрибутот е тоа што е immutable.

increment()
canEnrollAgain()

Горенаведните domain методи се domain primitives, што е добро. Единствено подобрување е тоа што едниот метод би го вгнездила во другиот, како на пример:

public NumberOfEnrollments increment() {
    if (!canEnrollAgain()) throw ...
}

        одговор: Променет  е increment()

GroupName name = new GroupName("Group A");

Добрите страни е тоа што има имплементирано ограничувања за должината на името на групата и тоа што има whitelist. Очигледно е дека имплементацијата е направена со anti-injection mindset, што е одлично бидејќи спречува SQL injections. 

Она што би го подобрила е тоа дека regex делот може да стане премногу ограничувачки, на пример со внесување на интернационални имиња. Сугестија за подобрување на ова би било дозволување на Unicode letters.

GroupId id = GroupId.randomGroupId();

        Одговор: Не би го сменил GroupID бидејќи добро е да има стрикнти ASCII, подобрува безбедност

Одлична е имплементацијата на UUID, што го прави методот не-секвенцијален. Исто така, применувањето на factory метода, допринесува до чист API.

Како што споменав претходно за другиот конструктор без параметри, подобрувањето би го направила со додавање на барем еден параметар во конструкторот. На пример:

public static GroupId fromString(String value)

enrollment.markAsInvalid(...)
enrollment.markAsValid()
enrollment.recordReEnrollment()

Горните методи го претставуваат најсилниот дел од дизајнот на кодот.

Единствено би сменила еден метод со тоа што наместо:

setValid(false)

ќе има

markAsInvalid(...)

        одговор: Не би променил бидејчи веќе е имплементирано во кодот


Ова ја крие логиката, гарантира invariant-и и спречува неконзистентна состојба.

new NumberOfEnrollments(-999); // throws

Со применување на исклучок директно во методот, се спречува внесувањето на погрешен или tainted податоци и ја елиминира потребата на користење на defensive checks подоцна во кодот.

notNull → range → regex

Ова е одличен тек на состојбите поради тоа што го зема перформансот во преден план.

NumberOfEnrollments six = five.increment();

Ова е чист, безбеден и предвидлив метод.

checkInvariants()

Добро е тоа што има централизирана проверка на invariant-ите. Истото важи и за покомплексните објекти во класата.

Но, ако не се повика checkInvariants() во сите конструктори, може да доведе до bugs.

Треба да се осигураме дека: constructor → always calls checkInvariants()

Главните подобрувања за кодот е тоа што треба конструкторите да се заменат со чисти factory методи. 

        одговор: Додадено е checkInvariants() во конструкторот на Product.java

На пример наместо new GroupId() да има GroupId.randomGroupId().

Треба да се додаде fromString() за сите ID primitives.

Не е добро да се потпира само на regex за безбедност, треба и слојот на базата на податоци да е исто така безбедна.

Поради практични загрижувања, треба да се намали max length кај сите text fields.

Треба да се осигураме дека invariant checks секогаш се повикуваат.

Ова е многу добро дизајниран domain model. Има одлични domain primitives, ја има карактеристиката на immutability, fail-fast и expressive API.

Единственото што фали е factory методи, boundary handling и пореалистични ограничувања за сите text fields.

Сите примитиви (како StudentSubjectEnrollmentId, InvalidNote, итн.) се добро дефинирани и валидирани при креирање.

Секој примитив има проверка за валидност и соодветен доменски правилник.

Не е експлицитно наведено како се реферираат другите агрегати и ентитети, но се претпоставува дека агрегатот Enrollment комуницира со други примитиви преку методите.

Агрегатот Enrollment е добро дефиниран преку јасни методи и операции како markAsInvalid() и assignToGroup().

Сите конструктори на примитивите се добро дефинирани и вршат валидирање при креирање на објектот.

Агрегатните методи се добро дефинирани, но можеби недостасуваат некои методи за други бизнис логики.

Креирањето на објектите на агрегатите е јасно, користејќи доменски примитиви за обезбедување на валидни податоци при создавање.

Состојбата на агрегатот е добро заштитена со валидацијата при креирање и операциите кои гарантираат заштита на состојбата.

Не постојат експонирани мутабилни колекции, поради тоа што сите примитиви и агрегати се иммутабилни.

Агрегатот работи како state entity, односно тој самостојно ја управува својата состојба.

Состојбите се исто така добро дефинирани, но може да се подобри документацијата за транзицијата на состојбите.

Кодот добро покрива важни аспекти на доменско-ориентираниот дизајн и сигурност, но може да се подобри документацијата и да се разјасни интеракцијата меѓу агрегатите.
